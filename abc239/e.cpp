#include <bits/stdc++.h>
using namespace std;
using ll=long long;

int main(){
    ll n, Q; cin >> n >> Q;
    vector<ll> x(n); for(ll i = 0; i < n; i++) cin >> x[i];
    vector<ll> a(n-1), b(n-1);
    
    for(ll i = 0; i < n-1; i++) cin >> a[i] >> b[i];
    
    for(ll q = 0; q < Q; q++){

    }

    return 0;
}