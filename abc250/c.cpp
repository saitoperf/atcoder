#include <bits/stdc++.h>
using namespace std;
using ll=long long;

int main(){
    ll n, q; cin >> n >> q;

    vector<ll> ans(n); for(ll i=0; i < n; i++) ans[i] = i+1;

    ll root = 0;
    for(ll i = 0; i < q; i++){
        ll x; cin >> x; x--;
        
    }

}