#include <bits/stdc++.h>
using namespace std;
using ll=long long;
using Graph = vector<vector<ll>>;

int main(){
    ll n, k; cin >> n >> k;
    vector<ll> a(k); for(ll i = 0; i < k; i++)cin >> a[i];

    set<ll> s;
    for(ll i = 1; i <= n; i++){
        if(s.size() > k) break;
        
    }
    return 0;
}